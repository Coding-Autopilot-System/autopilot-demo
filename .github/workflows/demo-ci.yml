name: Demo CI

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Demo sanity
        run: |
          echo "Simulated demo step (non-blocking)."
